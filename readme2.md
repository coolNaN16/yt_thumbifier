# HOW TO USE

1. Download the `app` folder
2. Add your chosen images to the `images` folder
     - Make sure the aspect ratio is 16:9
     - Name the images > (1, 2, 3, ...) > (i suggest bulk rename utilty)
4. Installation in `README.md`
